require 'spec_helper'

describe Brandcaptcha do
  it 'has a version number' do
    expect(Brandcaptcha::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
